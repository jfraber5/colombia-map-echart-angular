import { Routes } from '@angular/router';
import { MapColombiaComponent } from './map-colombia/map-colombia.component';
import { AppComponent } from './app.component';

export const routes: Routes = [
  { path: 'mapa', component: MapColombiaComponent },
];
